import 'package:flutter/widgets.dart';

class DriverControl extends StatelessWidget {
  const DriverControl({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
